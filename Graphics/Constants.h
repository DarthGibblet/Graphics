#pragma once

const double MAX_JUMP_HOLD_DURATION = 0.5; //Maximum number of seconds holding the jump button is meaningful
const double BASE_JUMP_STRENGTH = 4.5; //Base velocity upward when jumping
const double BULLET_SPEED = 7; //Default velocity of bullets when fired
