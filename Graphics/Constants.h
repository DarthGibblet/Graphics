#pragma once

const double GRAVITY_STRENGTH = 10; //Strength of gravity on object (how fast they accellerate)
const double MAX_JUMP_HOLD_DURATION = 0.4; //Maximum number of seconds holding the jump button is meaningful
const double BASE_JUMP_STRENGTH = 6; //Base velocity upward when jumping
const double BULLET_SPEED = 7; //Default velocity of bullets when fired
const double MAX_PLAYER_WALKING_SPEED = 5; //Fasted a player can go while just walking in one direction

const int STENCIL_BUFFER_COLLISION_DETECTION_MASK = 1; //Needs 1 bit of space
